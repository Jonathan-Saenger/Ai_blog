<section class="presentation" id="presentation">
    <h2 class="section-title neon-text">Développeur à l'ère de l'IA : S'adapter, Innover, Sécuriser</h2>
    <p class="section-intro">L'intelligence artificielle révolutionne le développement web. Loin d'être une menace, elle offre aux développeurs une opportunité unique d'optimiser leur code, d'accélérer le processus de création et d'offrir aux professionnels de nouveaux outils pour répondre aux mutations de leur marché. Mais ces avancées soulèvent aussi un défi majeur : la cybersécurité.</p>

    <div class="presentation-content">
        <div class="text-content">
            <h3>Jonathan Saenger, développeur web à l'ère de l'IA</h3>
            <p>Je suis Jonathan Saenger, développeur web passionné par l'évolution technologique. Je suis heureux de vous partager de nouvelles pratiques de développement à l'ère de l'intelligence artificielle. Développeur web fullstack, j'étudie actuellement le Cloud Computing et me forme sur l'Intelligence Artificielle. Tout cela me permet d'explorer comment notre façon de travailler est en pleine transformation avec la sécurité, plus que jamais, placé au coeur de notre priorité.</p>
            <p>À travers ce blog, je partage ma vision d'un développement web moderne où l'IA devient un allié puissant pour doper notre apprentissage, optimiser nos processus de développement et stimuler notre créativité. Que vous soyez un professionnel et/ou passionné de tech, j'essaierais de vous partager autant de ressources utiles pour naviguer dans cette ère numérique en pleine transformation.</p>
            <a href="#latest-articles" class="read-more">Découvrir mes articles</a>
        </div>
        <div class="image-content">
            <div class="portrait-container">
                <img src="{{ asset('images/portrait.png') }}" alt="Portrait de Jonathan Saenger" class="portrait-image">
            </div>
        </div>
    </div>
</section>

<style>

.presentation {
    padding: 2rem 4%;
    max-width: 1440px;
    margin: 1.5rem auto;
    position: relative;
    z-index: 2;
    background: linear-gradient(15deg, #f8f9fa 0%, rgba(248, 249, 250, 0.95) 100%);
    border-radius: 16px;
    backdrop-filter: blur(8px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    border: 1px solid rgba(111, 66, 193, 0.2);
    overflow: hidden;
  }

  /* Grille de fond cyber */
  .presentation::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
        linear-gradient(to right, rgba(0, 123, 255, 0.08) 1px, transparent 1px),
        linear-gradient(to bottom, rgba(111, 66, 193, 0.08) 1px, transparent 1px),
        repeating-linear-gradient(
            45deg,
            transparent 0%,
            transparent 2px,
            rgba(232, 62, 140, 0.07) 3px,
            rgba(232, 62, 140, 0.07) 4px
        );
    background-size: 40px 40px, 40px 40px, 100% 100%;
    transform: perspective(1000px) rotateX(60deg);
    opacity: 0.7;
    z-index: -2;
  }

  /* Particules statiques */
  .presentation::after {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
        radial-gradient(
            circle at 30% 20%,
            rgba(0, 123, 255, 0.15) 0%,
            transparent 30%
        ),
        radial-gradient(
            circle at 70% 80%,
            rgba(232, 62, 140, 0.15) 0%,
            transparent 30%
        ),
        radial-gradient(
            circle at 50% 50%,
            rgba(111, 66, 193, 0.1) 0%,
            transparent 40%
        ),
        radial-gradient(
            circle at 70% 20%,
            rgba(232, 62, 140, 0.15) 0%,
            transparent 30%
        ),
        radial-gradient(
            circle at 30% 80%,
            rgba(0, 123, 255, 0.15) 0%,
            transparent 30%
        );
    opacity: 0.5;
    z-index: -1;
  }

  .section-title {
    font-weight: 800;
    font-size: 2.3rem;
    background: linear-gradient(45deg, #007bff, #6f42c1, #e83e8c);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    display: inline-block;
    margin-bottom: 0.8rem;
    text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.15);
    letter-spacing: -0.5px;
  }

  .section-intro {
    color: #495057;
    max-width: 85%;
    margin-bottom: 1.5rem;
    line-height: 1.6;
    font-size: 1.05rem;
  }

  .presentation-content {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 3rem;
    align-items: center;
    margin-top: 1.5rem;
    padding: 1.8rem;
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 0.7));
    border-radius: 12px;
    backdrop-filter: blur(5px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  /* Effet de brillance sur les bords */
  .presentation-content::before {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(45deg,
        rgba(0, 123, 255, 0.3),
        rgba(111, 66, 193, 0.3),
        rgba(232, 62, 140, 0.3)
    );
    border-radius: 14px;
    z-index: -1;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .presentation-content:hover::before {
    opacity: 1;
  }

  .text-content {
    padding: 0.8rem;
    position: relative;
  }

  .image-content {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 0.8rem;
  }

  .portrait-container {
    position: relative;
    overflow: hidden;
    border-radius: 12px;
    box-shadow: 0 4px 25px rgba(0, 123, 255, 0.2);
    transition: transform 0.3s ease;
  }

  .portrait-container:hover {
    transform: translateY(-5px);
  }

  .portrait-image {
    width: 100%;
    max-width: 380px;
    height: auto;
    border-radius: 12px;
    transition: transform 0.5s ease;
    object-fit: cover;
  }

  .portrait-container:hover .portrait-image {
    transform: scale(1.05);
  }

  .text-content h3 {
    font-size: 1.7rem;
    margin: 1.2rem 0;
    background: linear-gradient(45deg, #007bff, #6f42c1);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    display: inline-block;
    letter-spacing: -0.3px;
  }

  .text-content p {
    color: #495057;
    margin: 0.8rem 0;
    line-height: 1.7;
    font-size: 1.02rem;
  }

  .read-more {
    display: inline-block;
    margin-top: 1.2rem;
    padding: 0.6rem 1.4rem;
    background: linear-gradient(45deg, #007bff, #6f42c1);
    color: white;
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.3s ease;
    font-weight: 500;
    position: relative;
    z-index: 1;
    overflow: hidden;
    font-size: 0.95rem;
    letter-spacing: 0.3px;
  }

  .read-more::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 0;
    height: 100%;
    background: linear-gradient(45deg, #0069d9, #5a36a0);
    transition: width 0.3s ease;
    z-index: -1;
  }

  .read-more:hover::before {
    width: 100%;
  }

  .read-more:hover {
    transform: translateX(5px);
    box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
  }

  @media screen and (max-width: 768px) {
    .section-intro {
        max-width: 100%;
        font-size: 1rem;
    }

    .presentation-content {
        grid-template-columns: 1fr;
        gap: 1.5rem;
        padding: 1.5rem;
    }

    .image-content {
        order: -1;
    }

    .portrait-image {
        max-width: 100%;
    }

    .presentation {
        padding: 1.5rem 4%;
        margin: 1rem auto;
    }

    .text-content h3 {
        font-size: 1.5rem;
    }

    .text-content p {
        font-size: 1rem;
    }

    .presentation::before {
        transform: perspective(800px) rotateX(60deg);
        background-size: 30px 30px, 30px 30px, 100% 100%;
    }
  }

</style>